{
  "buildCommand": "cd tutor-frontend && npm install && npm run build",
  "outputDirectory": "tutor-frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
